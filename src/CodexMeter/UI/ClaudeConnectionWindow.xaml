<Window x:Class="CodexMeter.UI.ClaudeConnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:CodexMeter.UI"
        Style="{StaticResource AppWindow}" Width="670" Height="700" MinWidth="470" MinHeight="400"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize" PreviewKeyDown="OnKeyDown">
    <Window.Resources>
        <Style x:Key="GuideText" TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="LineHeight" Value="19"/>
            <Setter Property="Margin" Value="0,9,0,0"/>
        </Style>
    </Window.Resources>
    <Border Background="{DynamicResource BgBrush}" BorderBrush="{DynamicResource LineBrush}"
            BorderThickness="1" CornerRadius="14" Padding="22">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <StackPanel MouseLeftButtonDown="OnDrag">
                <TextBlock x:Name="Heading" FontSize="22" FontWeight="SemiBold"/>
                <DockPanel Margin="0,10,0,14">
                    <ui:UsageProviderBadge Provider="Claude" DockPanel.Dock="Left"/>
                    <TextBlock x:Name="ProfileName" Margin="8,0,0,0" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                </DockPanel>
            </StackPanel>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="0,0,8,0">
                    <TextBlock x:Name="SetupSteps" Style="{StaticResource GuideText}"/>
                    <TextBox x:Name="ConnectionJson" IsReadOnly="True" FontFamily="Consolas" FontSize="11"
                             TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Disabled" Height="170" Padding="9" Margin="0,12,0,0"/>
                    <Button x:Name="CopyButton" Style="{StaticResource CreditUseButton}" Click="OnCopy" HorizontalAlignment="Left" Margin="0,10,0,0"/>
                    <TextBlock x:Name="ExistingStatusLineHint" Style="{StaticResource GuideText}"/>
                    <TextBlock x:Name="IdentityHint" Style="{StaticResource GuideText}"/>
                    <TextBlock x:Name="FreshnessHint" Style="{StaticResource GuideText}"/>
                    <Button x:Name="DocsButton" Style="{StaticResource CreditUseButton}" Click="OnDocs" HorizontalAlignment="Left" Margin="0,12,0,4"/>
                </StackPanel>
            </ScrollViewer>
            <DockPanel Grid.Row="2" Margin="0,14,0,0">
                <Button x:Name="DoneButton" DockPanel.Dock="Right" Style="{StaticResource CreditUseButton}" Click="OnDone" Margin="12,0,0,0"/>
                <TextBlock x:Name="CopyStatus" Style="{StaticResource GuideText}" VerticalAlignment="Center" Margin="0"/>
            </DockPanel>
        </Grid>
    </Border>
</Window>
