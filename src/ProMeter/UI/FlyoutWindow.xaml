<Window x:Class="ProMeter.UI.FlyoutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ProMeter"
        Width="320"
        SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        Deactivated="OnDeactivated"
        PreviewKeyDown="OnPreviewKeyDown">
    <Border Background="{StaticResource CardBrush}"
            BorderBrush="{StaticResource LineBrush}"
            BorderThickness="1"
            CornerRadius="14"
            Padding="16">
        <StackPanel>
            <DockPanel>
                <TextBlock Text="ProMeter" FontWeight="SemiBold" FontSize="16" Foreground="{StaticResource TextBrush}"/>
                <TextBlock x:Name="StatusText" DockPanel.Dock="Right" HorizontalAlignment="Right"
                           Foreground="{StaticResource MutedBrush}" FontSize="11"/>
            </DockPanel>

            <TextBlock Style="{StaticResource SectionTitle}" Text="GPT PRO"/>
            <DockPanel>
                <TextBlock x:Name="ProCountText" DockPanel.Dock="Right" Foreground="{StaticResource TextBrush}" FontWeight="SemiBold"/>
                <Border Height="8" CornerRadius="4" Background="#2A3140" Margin="0,4,12,0">
                    <Border x:Name="ProBar" Width="0" HorizontalAlignment="Left" CornerRadius="4" Background="{StaticResource AccentBrush}"/>
                </Border>
            </DockPanel>
            <DockPanel Margin="0,8,0,0">
                <TextBlock Text="Remaining" Foreground="{StaticResource MutedBrush}"/>
                <TextBlock x:Name="RemainingText" DockPanel.Dock="Right" HorizontalAlignment="Right" Foreground="{StaticResource TextBrush}"/>
            </DockPanel>
            <ItemsControl x:Name="ModelRows" Margin="0,8,0,0"/>

            <TextBlock Style="{StaticResource SectionTitle}" Text="SOL REASONING"/>
            <DockPanel><TextBlock Text="Today" Foreground="{StaticResource MutedBrush}"/><TextBlock x:Name="ReasonToday" DockPanel.Dock="Right" HorizontalAlignment="Right"/></DockPanel>
            <DockPanel Margin="0,4,0,0"><TextBlock Text="This week" Foreground="{StaticResource MutedBrush}"/><TextBlock x:Name="ReasonWeek" DockPanel.Dock="Right" HorizontalAlignment="Right"/></DockPanel>
            <DockPanel Margin="0,4,0,0"><TextBlock Text="Medium" Foreground="{StaticResource MutedBrush}"/><TextBlock x:Name="ReasonMedium" DockPanel.Dock="Right" HorizontalAlignment="Right"/></DockPanel>
            <DockPanel Margin="0,4,0,0"><TextBlock Text="High" Foreground="{StaticResource MutedBrush}"/><TextBlock x:Name="ReasonHigh" DockPanel.Dock="Right" HorizontalAlignment="Right"/></DockPanel>
            <DockPanel Margin="0,4,0,0"><TextBlock Text="Extra High" Foreground="{StaticResource MutedBrush}"/><TextBlock x:Name="ReasonExtra" DockPanel.Dock="Right" HorizontalAlignment="Right"/></DockPanel>
            <DockPanel Margin="0,4,0,0"><TextBlock Text="Limit" Foreground="{StaticResource MutedBrush}"/><TextBlock x:Name="ReasonLimit" DockPanel.Dock="Right" HorizontalAlignment="Right"/></DockPanel>

            <TextBlock Style="{StaticResource SectionTitle}" Text="STATUS"/>
            <DockPanel><TextBlock Text="Reset" Foreground="{StaticResource MutedBrush}"/><TextBlock x:Name="ResetText" DockPanel.Dock="Right" HorizontalAlignment="Right"/></DockPanel>
            <DockPanel Margin="0,4,0,0"><TextBlock Text="Last sync" Foreground="{StaticResource MutedBrush}"/><TextBlock x:Name="SyncText" DockPanel.Dock="Right" HorizontalAlignment="Right"/></DockPanel>
            <Button Click="OnCoverageClick" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Stretch" Cursor="Hand" Margin="0,4,0,0">
                <DockPanel>
                    <TextBlock Text="Coverage" Foreground="{StaticResource MutedBrush}"/>
                    <TextBlock x:Name="CoverageText" DockPanel.Dock="Right" HorizontalAlignment="Right" Foreground="{StaticResource AccentBrush}"/>
                </DockPanel>
            </Button>
        </StackPanel>
    </Border>
</Window>
